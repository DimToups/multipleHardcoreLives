CREATE TABLE IF NOT EXISTS session (
	"server"	TEXT NOT NULL,
	"sessionNumber"	INTEGER NOT NULL,
	"sessionStart"	DATE,
	"sessionEnd"	DATE,
	PRIMARY KEY("sessionNumber","server"),
	FOREIGN KEY("server") REFERENCES "server"("address")
)